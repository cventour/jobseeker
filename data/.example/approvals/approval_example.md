---
id: appr_example
kind: apply
status: pending
created: 2026-07-10T08:00:00Z
related_id: prop_example
channels: whatsapp, chat
summary: Submit application to Example Security Inc — Solution Architect
---

# Proposed action

`kind` is one of: apply, message. `status` is one of: pending, approved, edited, rejected.

The orchestrator only executes items with status `approved` or `edited`.

## Preview

For an `apply`: the form answers to be submitted.
For a `message`: the exact text to be sent, and to whom / which channel.

> Draft preview goes here. Edit this file's `status:` to `approved` (or `edited` after tweaking
> the preview) to authorize, or `rejected` to cancel. You can also approve from WhatsApp or chat.
